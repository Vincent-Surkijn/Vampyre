<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vincentsurkijn.softdev.kuleuven.vampyre">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_logo_rect"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_logo_rect"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Support"></activity>
        <activity android:name=".Survey" />
        <activity
            android:name=".newAppointment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".register" />
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Score_shop" />
        <activity android:name=".FactsArticle" />
        <activity android:name=".My_score" />
        <activity
            android:name=".Agenda"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.gms;version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>